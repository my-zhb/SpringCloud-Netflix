server:
  port: 8000
eureka:
  instance:
    #告诉服务端，服务实例的名称
    instance-id: springcloud-service-protal-01
spring:
  cloud:
    config:
      profile: devportal
      label: master
      uri: http://127.0.0.1:8888/